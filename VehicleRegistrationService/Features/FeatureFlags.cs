namespace VehicleRegistrationService.Features;

internal static class FeatureFlags
{
    public const string DisableConfEndpoint = nameof(DisableConfEndpoint);
    public const string JwtJtiReplayControl = nameof(JwtJtiReplayControl);
}
